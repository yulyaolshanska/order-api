/** @type {import("prettier").Config} */

const config = {
  arrowParens: "always",
  bracketSpacing: true,
  printWidth: 80,
  quoteProps: "preserve",
  semi: true,
  singleQuote: false,
  tabWidth: 2,
  useTabs: true,
};

module.exports = config;
